{
  "name": "ishara",
  "version": "1.0.0",
  "description": "منصة لتسجيل وترجمة لغة الإشارة العربية عبر الكاميرا، بدعم عدة لهجات عربية.",
  "private": true,
  "scripts": {
    "build": "npm install --prefix web && npm install --prefix server && npm run build --prefix web && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "dev:web": "npm run dev --prefix web",
    "dev:server": "npm run dev --prefix server"
  },
  "license": "ISC"
}
